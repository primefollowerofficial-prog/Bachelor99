'use strict';
/* ============================================
   Global frontend configuration
   Loaded before any other script on every page
   (index.html, cart.html, thankyou.html)
   ============================================ */

// Your live Railway backend.
const API_BASE_URL = 'https://bachelor99-backend-production.up.railway.app';

/*
  REPLACE THIS once you tell me your real custom domain:
  e.g. if your site is https://www.bachelor99.com, that exact origin
  needs to be added to Railway's ALLOWED_ORIGINS variable so CORS allows it.
  Nothing to change here in config.js for that — this file only needs the
  backend URL above, which is already correct.
*/